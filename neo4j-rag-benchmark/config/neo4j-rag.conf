# Keep built-in procedures available for the baseline and enable rag.retrieve.
dbms.security.procedures.allowlist=*
dbms.security.procedures.unrestricted=rag.*

# Disable auth for the local benchmark runtime so import verification and benchmark runs
# do not require interactive password setup.
dbms.security.auth_enabled=false
